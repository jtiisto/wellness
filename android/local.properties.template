# Copy this to local.properties and set your Android SDK path
sdk.dir=/home/YOUR_USER/android-sdk

# Optional: override the Wellness server base URL compiled into
# BuildConfig.WELLNESS_BASE_URL (:core:data). Must include the /wellness path
# prefix — endpoint paths are appended to it.
# Default (tailscale serve): https://pop-os.tailexample.ts.net:9443/wellness
# Local dev server:          http://<host>:9000/wellness
#wellness.baseUrl=http://192.168.1.10:9000/wellness
